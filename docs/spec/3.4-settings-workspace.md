# 3.4 Settings Workspace

> **Revision status.** First revision. This section specifies the Settings Workspace as
> established in the wireframe and prototype review of 3 Sep 2026 and built since. It is
> written to close a gap: §3.2.3.10 cites §3.4.8 as the source of the Library switcher's
> list, and no §3.4 existed.
>
> **Amended 4 Sep 2026** — §3.4.8.1 Databases and §3.4.8.2 Engines added, specifying the
> row pattern accepted that day (`wireframes/settings-databases.html` Rev D and
> `wireframes/settings-engines.html` Rev A), both built into the prototype.
>
> **Amended again 4 Sep** — §3.4.8.3 Subscriptions added (`wireframes/settings-subscriptions.html`
> Rev A, option SU‑A), built into the prototype.
>
> **All three object sections now use rows.** The Card pattern described in §3.4.8 and
> §3.4.10 is consequently **not rendered by any section**; see the note at the end.
>
> On the section number, see the note at the end.

---

## 3.4.1 Overview

The Settings Workspace is the application's configuration surface. It presents application preferences and the three collections of user-configured objects — Engines, Subscriptions and Databases — that the rest of the application draws on.

It is a workspace like any other (§3.1) and occupies the Workspace Area when the Settings Tab is active.

**Settings auto-apply.** Every change commits the moment it is made. There is **no Save button, no Cancel, and no draft state**, and consequently no exit route that can strand unsaved work.

Three requirements follow from auto-apply, and none is optional:

1. **Validation happens before the commit**, not at a save step, because there is no save step to fail at.
2. **Destructive actions carry their own confirmation**, because there is no Cancel to walk them back.
3. **Leaving a section, or closing the tab, needs no prompt.** Nothing is pending.

Settings changes take effect immediately throughout the application. A database disabled here disappears from the Library switcher at once (§3.2.3.10), with no intervening step.

---

## 3.4.2 Layout

The Settings Workspace is divided horizontally into two regions:

```text
┌──────────────────────┬──────────────────────────────────────────────┐
│                      │                                              │
│  Settings Sidebar    │                Content Area                  │
│                      │                                              │
└──────────────────────┴──────────────────────────────────────────────┘
```

The **Settings Sidebar** occupies the left side at a fixed **220px**, matching the Library Workspace's expanded Sidebar (§3.2.2) so that switching tabs does not shift the boundary between navigation and content.

The **Content Area** occupies all remaining horizontal space.

**The Content Area is the only scrolling region.** The Sidebar does not scroll; see §3.4.4.

Unlike the Library Sidebar, **the Settings Sidebar does not collapse**. It carries a short, fixed list that always fits, so a collapse control would offer a state with no benefit, and an icon rail would drop labels the sections need.

---

## 3.4.3 Navigation

**There is at most one Settings Tab** (§2.1.1). Nothing in this workspace opens a second tab: Detail Views, confirmations and object creation all happen inside the one tab.

Selecting a section in the Sidebar changes the Content Area. It does not navigate the shell.

**The active section is remembered.** Closing the Settings Tab and reopening it returns to the section last viewed rather than resetting. The default on first open is **General**.

The active section is indicated **without relying on colour alone** (§9.3): the active row carries a leading rail and a heavier weight in addition to its fill.

---

## 3.4.4 Settings Sidebar

The Sidebar lists the workspace's sections, in this fixed order:

```text
┌─────────────────────────────┐
│   General                   │
│   Appearance                │
│   Engines                   │
│   Subscriptions             │
│   Databases                 │
│   About                     │
└─────────────────────────────┘
```

**The order is fixed and is not reordered by use, recency or alphabet.** Two conventional preference sections come first, the three object-management sections follow as a group, and About sits last. A settings list whose order moves is a settings list that cannot be learned.

The list is **flat**. There are no groups, no headings and no nesting: six items do not need an information architecture.

Each item renders on a single line with a leading icon, following §3.2.3.6's rendering rules and §7.4's icon vocabulary. The glyph is bound to the section rather than to the component, so a section cannot be added without being given one:

| Section | Glyph | |
| --- | --- | --- |
| General | `sliders-horizontal` | The gear is **not** used here. §2.2's Application Menu already spends `settings` on the Settings Tab itself, so the gear denotes the workspace, not a section within it |
| Appearance | `palette` | `sun` and `moon` are the menu's theme items, so `sun-moon` would half-collide |
| Engines | `cpu` | |
| Subscriptions | `rss` | Shared with a Feed in the Library Sidebar (§3.2.3.1). The same object, so §7.4 requires the same glyph |
| Databases | `database` | Shared with the Library (§3.2.3.10). The same object |
| About | `info` | Shared with the Application Menu's About item (§2.2) |

The glyph is drawn at **18px in an 18px slot** (§7.4.1), matching an object row's leading glyph — both sit on a 44px row, and a Sidebar at a different size would put the two regions out of step.

The Sidebar carries **no counts**. The number of configured engines is not navigational information.

**The Sidebar does not scroll in practice.** Six 44px items occupy 285px against the 560px available at the 600px window floor, and the list is fixed — a seventh section is a specification change, at which point this clause is revisited.

The component nevertheless sets `overflow-y: auto` as a defensive fallback, so that a height the list cannot fit degrades to a scrollbar rather than to clipped items. This is deliberate and is **not** a licence to grow the list: the clause above governs, and the fallback exists only so that an unforeseen height fails safely.

---

## 3.4.5 Content Area

The Content Area presents the active section.

It begins with a **Section Heading** naming the section. For object sections the heading also carries the section's **Add action** (§3.4.10).

Below the heading, the section's content is one of three kinds:

| Kind | Sections | Presentation |
| --- | --- | --- |
| Controls | General, Appearance | Setting Rows (§3.4.6) |
| Objects | Databases, Engines, Subscriptions | Rows in boxed groups (§3.4.8.1, §3.4.8.2, §3.4.8.3) |
| Informational | About | Static content (§3.4.11) |

The Content Area has **22px padding on each side**.

**The Content Area is the workspace's only scrolling region** (§3.4.12).

---

## 3.4.6 General

General carries application-level preferences that belong to no other section.

Established settings:

| Setting | Control | Notes |
| --- | --- | --- |
| Language | Select | Applies immediately to every open workspace and persists (§8). Also on the Application Menu, as a shortcut to this setting — see §3.4.7 |

*The Notes column above is specification prose. It is **not** rendered: no Setting Row carries a description (§3.4.13).*
| Restore open games on launch | Toggle | Whether Game Tabs reopen with the application |
| Library location | Path | Where the application keeps its data |

The section presents **three boxed groups**, each a rounded, bordered container with its label above it — the same treatment §3.4.8 gives the object sections:

```text
STARTUP
┌──────────────────────────────────────────────────────────┐
│ Restore open games on launch                        ( ●) │
└──────────────────────────────────────────────────────────┘

LANGUAGE
┌──────────────────────────────────────────────────────────┐
│ Language                                    [ English ⌄] │
└──────────────────────────────────────────────────────────┘

STORAGE
┌──────────────────────────────────────────────────────────┐
│ Library location          ~/Chessgui         [ Change… ] │
└──────────────────────────────────────────────────────────┘
```

Each setting renders as a **Setting Row** (§3.4.13). Controls commit on interaction, per §3.4.1.

**Library location is reported, not typed.** It shows the current path in the monospace face with a **Change…** button beside it. A free-text path invites one that does not exist, and §3.4.1's auto-apply means there is no Save at which to validate it. This is the shape §3.4.8.2's expander already uses for an engine's `Location`.

---

## 3.4.7 Appearance

Appearance carries presentation preferences, in **two boxed groups** — *Theme*, then *Board* — on the pattern of §3.4.6.

| Setting | Control |
| --- | --- |
| Theme | Select |
| Board style | Select |
| Piece set | Select |

**Settings is where a preference lives. The Application Menu is a shortcut to it.**

Theme appears here and in the menu; Language appears in General (§3.4.6) and in the menu (§2.2). This is deliberate. The menu carries the two preferences worth changing without leaving a workspace; Settings is where a user looks for a preference by name, and it is the canonical home of both.

**Both controls write the same value.** There is one preference reachable two ways, not two preferences. A change made in either place is immediately reflected in the other, and neither is a copy of the other's state.

> *Amended 10 Sep 2026.* This replaces the clause "Theme and Language are not here", which excluded both from Settings on the grounds that duplicating them "would create two controls for one value". The objection does not survive examination: one value reached from two places is the ordinary desktop pattern, the prototype has always written both through the same store, and excluding them left Appearance holding two rows that were both about the board.

---

## 3.4.8 Object Sections

Engines, Subscriptions and Databases share one object model — a name, a status, an enabled flag, and type-specific fields. **All three use rows** — Databases §3.4.8.1, Engines §3.4.8.2, Subscriptions §3.4.8.3.

An **object** is a user-configured resource that the application uses elsewhere: an engine to analyse with, a subscription to fetch games from, a database to read games out of.

Each object has:

| Property | Meaning |
| --- | --- |
| **Name** | User-facing label. Shown wherever the object appears in the application |
| **Status** | What the application currently makes of it — see below |
| **Enabled** | Whether the rest of the application may use it |
| **Fields** | Type-specific configuration (§3.4.9) |

**Status is reported, not chosen.** It describes what the application found, and the user cannot set it. The vocabulary is per type:

```text
Engines        not configured · ready · error
Subscriptions  not configured · syncing · offline · error
Databases      not configured · indexing · indexed · error
```

**Status and Enabled are independent.** A database can be indexed but disabled, and a disabled object is not an error. §3.2.3.10 depends on this distinction: the Library switcher lists every configured database and offers only those both `indexed` and enabled.

**Every object section is presented as rows** in boxed groups. See §3.4.8.1, §3.4.8.2 and §3.4.8.3.

The **Card** grid formerly described here, and its geometry in §3.4.10, are retained for reference but are **not used by any section**. See the appendix.

The row's leading slot is shared by all three sections and is specified once, in **§3.4.8.4**.

**Adding an object creates it and opens its configuration** in one step, with a default name and `not configured` status. A newly created object is **disabled by default**: an unconfigured resource must not be handed to the rest of the application before it has been given a path or a URL.

**Removing an object is destructive and not undoable**, and is confirmed before it happens (§3.4.1). Removal takes effect immediately everywhere; see §3.2.3.10 for what the Library switcher does when the active library is the object removed.

### 3.4.8.1 Databases

Databases use a **row pattern** rather than the Card grid. A database is chosen by comparing figures — how many games, how many players, how large — and a row aligns those figures into a column that can be read down. A grid of Cards does not.

Accepted 4 Sep 2026; the reference is `wireframes/settings-databases.html` Rev D.

#### Groups

The section presents two groups, each a **rounded, bordered box** with its label above it:

```text
INSTALLED
┌──────────────────────────────────────────────────────────────┐
│ ▤  Master Games      2.4M games · 198k players · 1.0 GB  ◉ › │
│ ▤  My Games            812 games · 24 players · 2.4 MB   ◉ › │
└──────────────────────────────────────────────────────────────┘

AVAILABLE
┌──────────────────────────────────────────────────────────────┐
│ ▤  Caissabase 2024  5.4M games · 321k players · 2.3 GB [Install] │
└──────────────────────────────────────────────────────────────┘
```

**Installed** are the databases registered with the application. **Available** is a curated list offered for download. Both are ordered alphabetically; neither marks how a database arrived.

#### The row

Rows are **44px** — Apple HIG's 44pt target, which satisfies WCAG 2.2 SC 2.5.5 (AAA) outright and is touch-ready without a second build.

| Slot | Installed | Available |
| --- | --- | --- |
| Leading icon | the database glyph, 18px in the §3.4.8.4 slot | the same |
| Name | the database's name | the catalogue entry's name |
| Detail | `games · players · size` | the same; the size is the download |
| Trailing | **toggle**, then **chevron** | **Install** button |

The toggle precedes the chevron. The toggle is a value control and belongs with the row's data; the chevron is structural and takes the outer edge.

**The detail line carries no status word.** An installed database is ready by definition, and the toggle already reports whether the application may use it.

Counts are compacted — `9.57M`, `526k` — and the size unit is chosen per value, because a personal database of a few hundred games and a distributed one of several million differ by four orders of magnitude.

#### Acquiring a database

A distributed database is a **Chessgui database file that is already indexed**. It downloads and it is ready: there is no indexing phase, and the `indexing` status in §3.4.8's vocabulary is not reached by this route.

**The Install button is the progress indicator.** It fills in place and reports a percentage; no separate progress bar appears, and the row does not change shape. On completion the entry shows `Installed` briefly, then leaves the Available list.

**A downloaded database arrives enabled.** This is a deliberate exception to §3.4.8's rule that new objects arrive disabled — that rule exists because an engine or a subscription cannot be used until it is given a path or a URL, and a downloaded database has nothing left to supply. It becomes selectable in the Library switcher (§3.2.3.10) immediately.

**Add database** registers a database file already on the machine. It is not an import: no games are copied and nothing is converted. It is distinct from Add games (§3.2.4.5), which imports games into the *active* library.

#### Row settings

The chevron **expands the row in place**; it does not navigate. The expanded area carries:

| Field | |
| --- | --- |
| Name | Editable. Commits on blur (§3.4.9). An empty name is refused rather than committed |
| Version | Reported, not set |
| Remove database | Destructive, confirmed (§3.4.1) |

The name is what the Library switcher displays and middle-truncates (§3.2.3.10), so renaming here renames the library throughout the application.

**Expanded state is momentary.** One row is expanded at a time, and the state is not persisted across visits to the section.

### 3.4.8.2 Engines

Engines use the **row pattern** of §3.4.8.1. Everything there holds — 44px rows, two boxed groups labelled *Installed* and *Available*, alphabetical order, the Add action in the section heading, the Install button doubling as the progress indicator, and a chevron that expands in place.

Accepted 4 Sep 2026; the reference is `wireframes/settings-engines.html` Rev A.

Two things differ, and both follow from **an engine being configurable where a database is not**.

#### The version sits beside the name

```text
INSTALLED
┌──────────────────────────────────────────────────────────┐
│ ⚙  Stockfish  17.1      UCI · 4 threads · 512 MB    ◉  › │
│ ⚙  Torch      3         UCI · 2 threads · 256 MB    ○  › │
└──────────────────────────────────────────────────────────┘
```

An engine's version is what distinguishes one release from another and is compared while scanning the list, so it is given a slot of its own immediately after the name, set in the **monospace face** so that releases align down the column.

A database carries its edition inside its name — *Caissabase 2024* — so §3.4.8.1 puts version in the expander instead. The two treatments are deliberate, not an inconsistency.

Engine detail lines are also much shorter than a database's, which is what leaves the room.

| Slot | Installed | Available |
| --- | --- | --- |
| Leading icon | the engine glyph (§7.4) | the same |
| Name | the engine's name | the catalogue entry's name |
| **Version** | beside the name, monospace | the same |
| Detail | `protocol · threads · hash` | `protocol · download size` |
| Trailing | **toggle**, then **chevron** | **Install** button |

The thread count is singular at one: `UCI · 1 thread · 256 MB`.

#### The expander carries controls

**An expander's sub-rows are 36px**, not §3.4.13's 44px. They sit *inside* an expanded row, and matching the parent would flatten the hierarchy that makes an expansion read as belonging to the row above it. This is the exception §3.4.13 refers to.

| Field | |
| --- | --- |
| Name | Editable. Commits on blur (§3.4.9) |
| Version | Reported, not set |
| **Threads** | Select. Commits on change |
| **Hash** | Select. Commits on change |
| Remove engine | Destructive, confirmed (§3.4.1) |

**Threads and Hash are the only engine options exposed.** They are the two the application itself sets on every engine. A UCI engine may declare dozens more; surfacing them all would make the expander's height unpredictable and turn a settings section into an engine console. Exposing further options is a specification change, not a configuration one.

#### Acquiring an engine

An engine is a binary: it downloads and it is ready. Acquisition is one phase, as for Databases.

**A downloaded engine arrives enabled**, with **1 thread** and **256 MB** hash. Like a downloaded database, it needs no path supplying, which is the reason §3.4.8 disables newly created objects — so the same exception applies.

**Add engine** registers an engine binary already on the machine.

### 3.4.8.3 Subscriptions

Subscriptions use the row pattern, in **one boxed group**.

Accepted 4 Sep 2026; the reference is `wireframes/settings-subscriptions.html` Rev A, option **SU‑A**.

#### There is no second group

§3.4.8.1 and §3.4.8.2 split their sections into *Installed* and *Available* because there is a catalogue to download from. **Subscriptions has no catalogue.** A subscription names something on one of two sources; nothing is fetched from a list, and no entry ever moves between groups. A second group would have nothing to hold.

Three alternatives were drawn and rejected:

| | Why not |
| --- | --- |
| Grouped by source | Borrows the two-group silhouette while meaning something different — a filter, not a state change. With one subscription per source it yields two boxes of one row each, and the shape changes with the user's data |
| A second group listing the two sources | Permanent, unlike *Available*, and duplicates the heading's Add action |
| Sync on the row | A third trailing control, more than any other row in Settings, for a rare action |

#### The row

```text
SUBSCRIPTIONS
┌────────────────────────────────────────────────────────────────────┐
│ ♟  Hikaru         Hourly · last synced 12 min ago            ◉   › │
│ ♞  AwesomeAtti    Daily · last synced 2 h ago       12 new   ◉   › │
│ ♞  DrNykterstein  Hourly · last synced 1 h ago      Syncing  ◉   › │
│ ♟  MagnusCarlsen  Weekly · never synced             Error    ○   › │
└────────────────────────────────────────────────────────────────────┘
```

| Slot | |
| --- | --- |
| Leading icon | The **source's own brand mark** — chess.com or lichess (§3.2.3.3, §7.4). Not a generic feed glyph, and not the engine or database icon |
| Name | The subscription's name, from its own configuration |
| Detail | `interval · last synced`, or `interval · never synced` |
| **Status** | Present only when a state applies — see below |
| Trailing | **toggle**, then **chevron** |

**The mark is what makes one group sufficient.** It already says which service a row belongs to, so a group heading would repeat the icon in words.

**Rows are sorted alphabetically by name**, as in the other two sections.

**There is no version field.** A subscription has no version; the slot Engines gained (§3.4.8.2) is simply absent.

#### Status is live

Engines and databases are effectively static once installed. **A subscription changes state while the section is open** — it syncs, it goes offline, it errors, it reports new games.

The status slot uses **the priority §3.2.3.3 already defines** for the Library Sidebar:

```text
error  ▸  syncing  ▸  new-game count  ▸  nothing
```

Reusing that ranking is not tidiness: it is what prevents one subscription from reading one way in Settings and another way in the Library.

**Error and syncing carry an icon as well as a colour** (§9.3). A new-game count is text, and appears only when non-zero.

#### Row settings

The chevron expands in place. The expanded area carries:

| Field | |
| --- | --- |
| Name | Editable. Commits on blur (§3.4.9). An empty name is refused rather than committed |
| Source | Reported, not set |
| Sync interval | Select — Hourly, Daily, Weekly, Manual. Commits on change |
| **Sync now** | The manual action no other object type has |
| Remove subscription | Destructive, confirmed (§3.4.1) |

**Sync now is in the expander, not on the row.** A subscription's whole purpose is that it syncs by itself, so a manual sync is a rare action and does not earn a permanent slot beside the toggle.

**A disabled subscription does not sync**, manually or otherwise. That is what disabling one means.

---


---

### 3.4.8.4 The leading slot

Every object row across all three sections opens with an **18px slot**, so the name column begins at the same offset throughout: 14px row padding + 18px slot + 12px gap = **44px**, which is also the expander's left indent.

The slot is a layout box and is not the same measurement as the glyph inside it. Databases and Engines draw a Lucide glyph at 18px; Subscriptions draws a Simple Icons brand mark at 16px. Both render the same visual height, for the reason §7.4.1 gives. Sizing the slot to its content instead put Subscriptions' names 2px left of every other section's and out of step with its own expander.

---

## 3.4.9 Detail / Edit View

Selecting a Card opens the object's **Detail View** in the Content Area, replacing the collection.

**The Sidebar stays live throughout.** Section navigation is never blocked by an open Detail View, and switching sections while one is open simply leaves it — there is nothing pending to lose (§3.4.1).

The Detail View lists the object's fields. Each field declares how it commits:

| Field type | Commits |
| --- | --- |
| Select, toggle | On change |
| Text | **On blur**, never per keystroke |

**Text fields commit on blur rather than as they are typed.** Auto-apply plus per-keystroke commit would apply every half-typed path, which is worse than useless for a value that names a file.

**Required fields are validated before commit** (§3.4.1). A field that fails validation is not committed and reports why, in place.

Established fields by type:

| Type | Fields |
| --- | --- |
| Engine | Path *(required)* · Hash · Threads · Enabled |
| Subscription | URL *(required)* · Update interval · Enabled |
| Database | Location *(required)* · Format · Enabled |

The Detail View carries the object's **Remove** action, with its confirmation.

There is no Save, no Cancel, and no prompt on exit. This is §3.4.1 applied, not a deviation from it.

---

## 3.4.10 Section Heading, Card Grid and Empty States

**The Section Heading carries the Add action**, labelled with the object type — `Add engine`, `Add subscription`, `Add database` — rather than a bare `Add`.

The Add action is **always present in the heading, whatever the object count**, including when the section is empty. A heading action that disappeared when the list emptied would remove the control at the moment it is most needed.

**Card grid geometry:**

| Measure | Value |
| --- | --- |
| Card minimum width | 210px |
| Gap between cards | 12px |
| Content Area padding | 22px each side |

Cards wrap into as many columns as fit. **Horizontal scrolling is never introduced** — at any width, at least one column renders and the grid wraps.

**Empty state.** A section with no objects shows a short explanation of what the object type is for, and a second Add control within the empty state itself. This is a second route to the heading's action, not the only one.

---

## 3.4.11 About

About presents application identity, the licences the application is bound by, and its credits. It is **informational**: nothing here is a configurable object and nothing carries a control, so **the Card pattern does not apply**.

**This section is an exception to §3.4.13.** Its content is not rows — it is a set of stated facts, and a fact is not a setting. The section is free to present them as it best can, and where a line needs to run long it does. It borrows §3.4.13's boxed groups, 44px rhythm and type so that it reads as the same family as every other section, but it is not bound by them, and a departure here is not a defect.

The Application Menu's About item (§2.2) opens the Settings Tab at this section rather than a separate dialog, so there is one place where this information lives.

The section presents **three boxed groups**.

### Application

| Fact | Value |
| --- | --- |
| Application | `Chessgui` |
| Version | **`Technology Preview (v0.1.0)`** — one localisable string with a `{version}` placeholder, per §8, rather than a label concatenated onto a number |
| Licence | **`GPL-3.0-or-later`** |
| Data location | that no data leaves the machine (§1.1) |

### Third party

One entry per redistributed component: its name, and its licence as an **SPDX identifier**.

| Component | Licence |
| --- | --- |
| Chessground | `GPL-3.0-or-later` |
| Svelte | `MIT` |
| Lucide | `ISC` |
| Simple Icons | `CC0-1.0` |
| IBM Plex | `OFL-1.1` |

**The application's licence is not a preference.** Chessground is GPL-3.0-or-later and carries no linking exception, and it is bundled into the application's JavaScript rather than shipped beside it, so the combined work is covered. Everything else listed imposes attribution only.

**Naming a licence does not discharge it.** MIT and GPL both require the licence *text* to accompany the software, and §1.1 requires the application to work offline, so the text is **bundled** rather than linked. The row that opens it is specified here but **not yet built** — see the appendix.

**Simple Icons carries no version** because it is not a dependency: two paths are copied into the source rather than installed (§7.4.2). CC0 waives copyright in the artwork and grants no trademark rights, which is why §7.4 confines a brand mark to denoting its own brand.

### Chessgui Credits

A single entry carrying the attribution and, beneath it, what the tools contributed:

> **Designed by AwesomeAtti. Brought to life with AI.**
> AI assisted with the code, wireframes, and documentation. The vision, direction, and design decisions courtesy of AwesomeAtti.

It carries a sentence rather than a value, so it takes the full width and flows: at the 800 × 600 floor (§2.4) it sets on two lines, which is accepted. It is **the one entry in the workspace with no right-hand column**. An attribution is a sentence rather than a value, so it takes the full width and is allowed to flow: at the 800 × 600 floor (§2.4) it sets on two lines, which is accepted. No reading-width cap is applied — one would wrap the line while the column still had room.

---

## 3.4.12 Resizing and Scrolling

**Every section scrolls, About included.** The scrolling region is the area **beneath** the Section Heading, not the Content Area as a whole.

**The Section Heading does not scroll.** It is pinned at 44px, and for an object section it carries the Add action (§3.4.10) — a header button that scrolled away would be unreachable with a dozen engines installed, which is the reason the action sits there rather than at the top of the first group.

The Settings Sidebar does not scroll (§3.4.4), and the Application Shell never scrolls (§2).

**Horizontal scrolling is never introduced** at any window size. The card grid reflows (§3.4.10), setting rows wrap their controls beneath their labels before overflowing, and the Detail View's fields are full-width.

At the 800 × 600 window floor (§2.4) the Content Area is 580px wide, which fits **two card columns** at the 210px minimum. The workspace is usable at the floor without a horizontal scrollbar and without the Sidebar collapsing.

---

## 3.4.13 The row

**Every row in the Settings Workspace is 44px**, whichever section it is in and whatever it carries. One height across the Sidebar, the Section Headings and every kind of content row is what puts the two regions on a single rhythm, so switching sections moves nothing on screen.

| Slot | Type | Rule |
| --- | --- | --- |
| Leading icon | 18px in an 18px slot | **Object rows only** (§3.4.8.4). It says which *kind* of thing the row is, which a control row does not need — every row in a control section is a preference |
| Label | 13.5px, weight 600 | The setting, object or field name |
| Detail / value | 12px, muted, right | Optional. Same line, never beneath |
| Control | right, fixed | Toggle, select, button, or the disclosure chevron |

**A row is one line.** Descriptions and tips are not part of this workspace's pattern: a setting whose label needs a sentence of explanation is a setting whose label is wrong. Anything a reader would need beyond the label belongs in the documentation, not the interface.

**Exceptions are documented where they occur.** Anything that departs from this clause says so in its own section and gives its reason. There are two: **§3.4.8's expander sub-rows**, which are subordinate to the row they open beneath, and **§3.4.11 About**, which presents stated facts rather than settings and is not row-based at all.

**One switch.** The toggle is 34 × 20 at a 10px radius, filled with the affirmative colour when on, everywhere in the workspace.

---

## Appendix — open items and a note on numbering

**Open items.**

1. **The Card pattern is no longer rendered by anything.** All three object sections use rows, so §3.4.8's Card description and §3.4.10's grid geometry, empty state and column arithmetic describe a presentation that does not appear. They are retained rather than deleted because the components still exist in the prototype and the Detail View of §3.4.9 is still reachable, but **§3.4.10 should be rewritten or retired** as a deliberate act rather than left to rot.
2. **The Detail View has no entry point.** §3.4.9 specifies opening it by selecting a Card; there are no Cards. It is reachable only programmatically. Either the row's chevron should lead there for some object types, or §3.4.9 should be folded into the three row clauses.
3. **What a subscription points at is unnamed.** Its `name` currently serves as both the display label and the thing it identifies. "Account" is wrong — it happens to fit chess.com and lichess, but a future source might identify a feed by URL or by event. A separate field is likely needed; the term is undecided.
4. **No keyboard specification.** §3.4 defines no keyboard behaviour, matching the gap §2.5's editor note records for the other workspaces.
5. **Engine options beyond Threads and Hash are unspecified** (§3.4.8.2), as is what an engine's `path` is used for now that the expander does not show it.
6. **The `indexing` status is now unreachable** by any specified route (§3.4.8.1). It is retained in §3.4.8's vocabulary against a future import path that would have to build an index, and §3.2.3.10's `Indexing…` row in the Library switcher likewise. Both should be revisited together if no such path appears.

**A note on the section number.**

This document is **§3.4**, which is what the prototype, the tests, and §3.2 all cite — more than forty references, including §3.2.3.10's dependency on §3.4.8. The numbering follows §3 *Workspaces* in the shell document: §3.1 Common Workspace Structure, §3.2 Library, §3.4 Settings.

Two inconsistencies are inherited rather than introduced:

- The shell document's revision note states that the workspaces are specified as **Library §4, Game §5, Settings §6**. That does not match the Library document, which is §3.2, and does not match any citation in the prototype. The note appears to predate the current numbering.
- The Game Workspace document is **§5**, where §3.3 would fit the §3 Workspaces scheme.

Both should be resolved in one pass rather than piecemeal, since either fix renumbers cross-references across every document, the prototype and its tests. **Unresolved.**
